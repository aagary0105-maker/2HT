import React from 'react';
import ReactDOM from 'react-dom/client';
import SecondhandTracker from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SecondhandTracker />
  </React.StrictMode>
);
